const letterPositions = function(sentence) {
    const results = {};


    for (let i = 0; i<sentence.length; i++)
    {
        var n = sentence.indexOf("g");
    }
    // logic to update results here
    console.log(n);
    return results;
  };

  letterPositions("lighthouse in the house")

  
